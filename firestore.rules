rules_version = '2';

// Каждый пользователь читает и пишет только свои собственные данные.
// users/{uid}            — catalog, currentWeekKey, currentDay
// users/{uid}/weeks/{weekKey} — план одной недели
// users/{uid}/diary/{dateKey} — запись дневника за конкретный день (текст + фото)
service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{uid} {
      allow read, write: if request.auth != null && request.auth.uid == uid;

      match /weeks/{weekKey} {
        allow read, write: if request.auth != null && request.auth.uid == uid;
      }

      match /diary/{dateKey} {
        allow read, write: if request.auth != null && request.auth.uid == uid;
      }
    }
  }
}
